server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    # Redireciona todas as rotas para o index.html
    # necessário para o React Router funcionar com slugs
    location / {
        try_files $uri $uri/ /index.html;
    }
}